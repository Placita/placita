version: "3.9"
services:
  node:
    restart: always
    container_name: placita-node
    image: index.docker.io/starlightromero/placita-node
    env_file:
      - .env
    ports:
      - 3000:3000
    networks:
      - placita-network

networks:
  placita-network:
    driver: bridge
